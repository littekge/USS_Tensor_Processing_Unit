import Operations

# define other imports

if __name__ == "__main__":
    # model parameters
    model_params = Operations.MODEL_PARAMS(
        MODEL=NN_NAME(),
        MODEL_NAME="NN_NAME",
        RUN_NAME="RUN_NAME",
        TRAIN = True,
        RUN = True,
        EXPORT = True
    )

    # training parameters
    training_params = Operations.TRAINING_PARAMS(
        SUBSET_SIZE=100,
        NUM_EPOCHS=2,
        BATCH_SIZE=4,
        LOSS_FUNCTION="MSE",
        CLASSIFICATION_MODE="regression"
    )

    # ---------- DEFINED PER MODEL ----------
    # define training data
    trainSet = 

    # define test data
    testSet = 

    # define sample data for export
    exportSet = 
    # ---------- END DEFINED PER MODEL ----------

    Operations.Start(
        model_params=model_params,
        training_params=training_params,
        testSet=testSet,
        trainSet=trainSet,
        exportSet=exportSet
    )